import React from "react"
import MainContent from "./MainContent"
import Hero from "../utils/Hero"
const Main = () => {
  return (
    <main>
      <Hero heroID={0} />
      <MainContent />
    </main>
  )
}
export default Main
